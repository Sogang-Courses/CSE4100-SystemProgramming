# myshell phase2: edirection and Pipe

## 목표
phase2 에서는 파이프를 사용해 여러 개의 명령어를 연결하고자 한다.

## 구현 방식
- eval()에서 파이프 기호(|)의 존재를 확인
- 파이프가 있을 경우 eval_pipeline() 함수를 재귀적으로 호출하여 각 명령을 분리 실행
- 각 명령어는 자식 프로세스를 통해 execvp() 를 호출하여 실행
- 파이프 단계가 2개 이상인 경우, 재귀적으로 연결

## 테스트 방식
make 후, ./myshell 명령어를 통해 테스트 가능합니다.
